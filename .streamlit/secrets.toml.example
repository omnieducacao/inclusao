# Copie este arquivo para secrets.toml e preencha os valores.
# NUNCA commite secrets.toml no Git.
#
# IMPORTANTE: Configure SUPABASE primeiro (login e dados dependem dele).
# Em Streamlit Cloud, coloque as chaves Supabase no topo para garantir
# que o login funcione mesmo em cold start.

# =============================================================================
# SUPABASE (obrigatório — login, estudantes, PEI, PAEE, workspaces)
# Configure PRIMEIRO. O app aceita chaves no nível raiz ou em [supabase].
# =============================================================================
# Formato 1 — no nível raiz (recomendado):
# SUPABASE_URL = "https://xxx.supabase.co"
# SUPABASE_SERVICE_KEY = "eyJ..."
# SUPABASE_ANON_KEY = "eyJ..."

# Formato 2 — seção [supabase] (também aceito):
# [supabase]
# url = "https://xxx.supabase.co"
# service_key = "eyJ..."
# anon_key = "eyJ..."

# =============================================================================
# Google Sheets (exportar Jornada Gamificada) — opcional, desativado por enquanto
# A exportação para planilha será retomada em versão futura.
# =============================================================================
# Opção 1: Caminho para o arquivo JSON da conta de serviço (desenvolvimento local)
# GOOGLE_SHEETS_CREDENTIALS_PATH = "/caminho/para/credenciais-google-sheets.json"

# Opção 2: JSON da conta de serviço como string (produção / Streamlit Cloud)
# Cole o conteúdo do arquivo JSON entre aspas triplas.
# GOOGLE_SHEETS_CREDENTIALS_JSON = """
# {"type": "service_account", "project_id": "...", "private_key_id": "...", "private_key": "-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n", "client_email": "...@....iam.gserviceaccount.com", ...}
# """

# Planilha de destino (opcional): todas as exportações vão para esta planilha, em uma nova aba.
# Compartilhe a planilha com o e-mail da conta de serviço (client_email do JSON).
# GOOGLE_SHEETS_SPREADSHEET_URL = "https://docs.google.com/spreadsheets/d/SEU_ID/edit?usp=sharing"
# GOOGLE_SHEETS_SPREADSHEET_ID = "SEU_ID"

# =============================================================================
# Teste local sem Supabase
# =============================================================================
# Com ENV=TESTE e sem chaves Supabase configuradas, use o botão "Entrar em modo demo"
# na tela de login para testar a interface (menu, navegação, layout).

# =============================================================================
# MOTORES DE IA (OmniRed, OmniBlue, OmniGreen, OmniYellow, OmniOrange)
# =============================================================================
# OmniRed (DeepSeek — mais utilizado: PEI, PAEE, Adaptar Provas, Hub)
# DEEPSEEK_API_KEY = "sk-..."
# DEEPSEEK_BASE_URL = "https://api.deepseek.com"  # opcional
# DEEPSEEK_MODEL = "deepseek-chat"  # opcional
#
# OmniBlue (Kimi — opção mais robusta: PEI, PAEE, Adaptar Provas, Hub)
# OPENROUTER_API_KEY = "sk-or-v1-..."  # ou KIMI_API_KEY
# OPENROUTER_BASE_URL = "https://openrouter.ai/api/v1"
# KIMI_MODEL = "moonshotai/kimi-k2.5"
#
# OmniGreen (Claude — apenas PEI)
# ANTHROPIC_API_KEY = "sk-ant-..."
# ANTHROPIC_MODEL = "claude-sonnet-4-20250514"  # opcional
#
# OmniYellow (Gemini — imagens, mapas mentais, Adaptar Atividades, Estúdio Visual)
# GEMINI_API_KEY = "..."
#
# OmniOrange (ChatGPT — reserva/fallback opcional)
# OPENAI_API_KEY = "sk-..."
